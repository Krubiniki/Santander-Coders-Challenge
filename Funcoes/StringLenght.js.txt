//utilizando o .lenght para realizar operações numéricas com a quantidade de caracteres de uma string.

function tamanhoNomeCompleto(nome, sobrenome){
    return (nome.length + sobrenome.length + 1);
}