//utilizar a palavra function para declarar funções em JS, depois o nome da função, acompanhado de (), onde ficam os parâmetros necessários para que a função seja executada com sucesso, em seguida, colocar dentro de {} comandos que essa função execute

function dobro(numero) {
   return numero*2;
}