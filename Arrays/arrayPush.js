//utilizando o .push em uma array para aumentar o tamanho dela adicionando ao novo último elemento

var pertences = ["espada","escudo","crossbow"];

console.log(pertences);
pertences.push("cross");
console.log(pertences);