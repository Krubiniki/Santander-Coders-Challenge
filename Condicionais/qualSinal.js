//função que checa se o número é positivo (retorna 1), negativo (retorna -1) ou zero (retorna 0)

function sinal(num)
{
    if (num === 0)
        return (0);
    else if (num > 1)
        return (1);
    else
        return (-1);
}